$$A \in R^{m, n} = U \Sigma V^T$$
- $U$: left singular vector (m dim)
- $V$: right singular vector (n-dim)
- $\Sigma$ : Singular values, D matrix of $\sigma$ values
How to obtain it?

We can decompose each A and its transpose in eigendecomposition between m and n. 