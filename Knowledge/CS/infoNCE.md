[[Contrastive Representation Learning ]]

It is inspired by the [[Noise Contrastive Estimation]], it uses categorical cross-entropy loss to identify the positive sample amongst a set of unrelated noise samples. 

Given a context vector $c$, the positive sample should be draw from the conditional distribution 