# Data

Pointers to where data and run artifacts live. This file is a lookup, not a story.

## Cluster paths

- Sequences / inputs: `…`
- Outputs / hidden reps: `…`

## Datasets

- `<name>` — source, size, location, format.

## Run IDs

- `<run-id>` — what it is, where outputs went.
