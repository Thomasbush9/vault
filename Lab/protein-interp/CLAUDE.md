# protein-interp — Claude context

Mechanistic interpretability of cofolding diffusion models (Boltz-2 primarily; AF3 / ESMFold as comparators). Active question: where in the model does the cheat-sheet effect live, and is the mutation signal recoverable from internal representations even when outputs look invariant?

## Where things are

- Code: see `repo.md`.
- Data (cluster paths, run IDs): `data.md`.
- Current focus: `agenda.md`.
- Latest experiment: most recent file in `log/`.
- History / dead ends / earlier experiment lists: `decisions.md`.
- Lit review: `lit.md`.

## Default load

Autoload only `agenda.md` and the most recent `log/` entry. Pull `decisions.md` / older logs / `lit.md` only when relevant.

## Cross-link

The companion GitHub repo for this project is documented in `repo.md`; Claude Code is launched there for the actual code work.
