# Agenda

Current focus and the next 1–3 concrete things. When done, fold into a `log/` entry; when parked, move to `decisions.md`.

## Now

- [ ] Replicate the PWA-conservation finding on 2–3 more proteins (not just GFP) — see open questions in `log/2026-05-04-pwa-conservation.md`.
- [ ] Probe 7: MI / APC-corrected MI per column, correlated with `A_j`. Does ρ flip positive when conservation is replaced by coevolution?
- [ ] Probe 8: structural-contact density per column, correlated with `A_j`.

## Next

- [ ] Perturbation gradient on same protein: p10/p20/p40/p70 paired with WT MSA; plot layer-3 JS(orig, mut) vs p.
- [ ] No-MSA control: rerun pipeline on Boltz with no MSA; check whether column-attention pattern collapses to query-driven.
- [ ] Layer-3 intervention: replace `random_uniform` PWA output at layer 3 with `original`'s, observe structural shift.

(Source: open questions at the end of `log/2026-05-04-pwa-conservation.md` and the recent items in `decisions.md`.)
