# Repo

GitHub repo + setup for protein-interp.

- **URL:** `<fill in>`
- **Local path on Mac Mini:** `<fill in>`
- **Local path on MBP:** `<fill in>`

## Notebooks

- `protein_interpretability/src/protein_interpretability/notebooks/pwa_conservation.ipynb` — PWA-conservation analysis.

## Cluster scripts

- `scripts/run_boltz_attention.py` — extracts PWA / pair / msa attention weights from Boltz runs. Config at `boltz_attention_config.yaml`.

## Companion: ProtForge

ProtForge is the pipeline used to generate inputs (sequences, MSAs, structure scoring). See `Lab/protforge/` for its own agenda.
