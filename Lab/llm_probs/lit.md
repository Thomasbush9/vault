# Literature

Scoped lit review. Group by sub-topic. For each entry: 1-line takeaway + 1-line "why it matters for us" + link.

## Seed paper — distributional behaviour of LLMs

1. **Large Language Models Are Bad Dice Players.** Behavioural eval of prompted frontier LLMs on sampling from named distributions; finds poor distributional fidelity, especially across stateless independent requests. *Why it matters:* this is the observation we are trying to move from "behavioural prompting of frozen models" to "controlled training + mechanistic analysis on tiny models". (TODO: add proper citation + link.)

## Probing / mechanistic interpretability

*(empty — to fill as we get to Q3 on hidden-state representations of mean/variance/modality/entropy.)*

## Masked vs. autoregressive generation

*(empty — relevant once we start the AR-vs-masked comparison; diffusion-style masking belongs here too.)*
