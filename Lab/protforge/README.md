# protforge

Tooling/pipeline used by `protein-interp` (sequence generation, MSA pipelines, structure scoring). Tracked here as a project so its TODOs and design notes don't pollute `protein-interp/`.

## Status

Active tool. Iteratively adding features as `protein-interp` needs them.
