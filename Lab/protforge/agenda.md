

## Things to add to ProtForge

1. Add hidden representation options 
2. add support for the uniprot upload : given the .csv we should extract the the sequences, write to a .fasta that then can be used for the sequence generation